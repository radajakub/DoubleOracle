# Exact solution by linear programming

```@docs
LinearProgram
```

To obtain an exact solution of a given Normal-Form game, use the function `solve` with `LinearProgram` parameter.

```@docs
solve(::NormalFormGame, ::Type{LinearProgram})
```
